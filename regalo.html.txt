<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Para Ti 🌸</title>
    <style>
        body {
            margin: 0;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100vh;
            background: linear-gradient(180deg, #ffafbd 0%, #ffc3a0 100%); /* Colores atardecer */
            font-family: 'Helvetica Neue', sans-serif;
            overflow: hidden;
            color: #fff;
        }

        #overlay {
            position: fixed;
            top: 0; left: 0; width: 100%; height: 100%;
            background: rgba(0,0,0,0.8);
            display: flex;
            justify-content: center;
            align-items: center;
            z-index: 100;
            transition: opacity 0.5s;
        }

        button {
            padding: 15px 30px;
            font-size: 1.2rem;
            border: none;
            border-radius: 25px;
            background-color: #ff4b2b;
            color: white;
            cursor: pointer;
            box-shadow: 0 4px 15px rgba(0,0,0,0.3);
        }

        #canvas {
            position: relative;
            width: 100%;
            height: 300px;
            display: flex;
            justify-content: center;
        }

        .snoopy-container {
            position: absolute;
            bottom: 20px;
            left: -120px;
            width: 120px;
            transition: all 1.5s cubic-bezier(0.25, 0.46, 0.45, 0.94);
        }

        .snoopy-img { width: 100%; }

        .heart, .flowers {
            position: absolute;
            top: -60px;
            left: 30px;
            font-size: 60px;
            display: none;
            text-shadow: 0 0 10px rgba(0,0,0,0.2);
        }

        .message {
            margin-top: 40px;
            font-size: 1.8rem;
            font-weight: bold;
            text-align: center;
            opacity: 0;
            transition: opacity 2s;
            text-shadow: 1px 1px 5px rgba(0,0,0,0.2);
        }

        @keyframes jump {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-20px); }
        }

        .jump { animation: jump 0.5s ease 2; }
    </style>
</head>
<body>

    <div id="overlay">
        <button onclick="startExperience()">Toca para abrir tu regalo 🎁</button>
    </div>

    <audio id="musica" loop>
        <source src="https://www.mboxdrive.com/continuo_atardecer.mp3" type="audio/mpeg">
        Tu navegador no soporta el audio.
    </audio>

    <div id="canvas">
        <div id="snoopy" class="snoopy-container">
            <div class="heart" id="heart">❤️</div>
            <div class="flowers" id="flowers">🌷🌼🌷</div>
            <img src="https://media.giphy.com/media/26vIg4xCXJlXQfXyM/giphy.gif" class="snoopy-img">
        </div>
    </div>
    
    <div id="text" class="message">
        Para la niña más especial... <br>
        💐 (Tulipanes y Gerberas para ti) 💐
    </div>

    <script>
        function startExperience() {
            document.getElementById('overlay').style.opacity = '0';
            setTimeout(() => document.getElementById('overlay').remove(), 500);
            
            const musica = document.getElementById('musica');
            musica.play();

            const snoopy = document.getElementById('snoopy');
            const heart = document.getElementById('heart');
            const flowers = document.getElementById('flowers');
            const text = document.getElementById('text');

            // 1. Snoopy camina al centro
            snoopy.style.left = 'calc(50% - 60px)';
            
            setTimeout(() => {
                // 2. Saluda con saltitos
                snoopy.classList.add('jump');
                
                setTimeout(() => {
                    // 3. Aparece el corazón
                    heart.style.display = 'block';
                    
                    setTimeout(() => {
                        heart.style.display = 'none';
                        // 4. Muestra las flores (Tulipanes y Gerberas)
                        flowers.style.display = 'block';
                        // 5. Muestra el mensaje
                        text.style.opacity = '1';
                    }, 2000);
                }, 1500);
            }, 1600);
        }
    </script>
</body>
</html>